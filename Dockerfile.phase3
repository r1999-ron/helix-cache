FROM node:24-bookworm-slim
ENV NODE_ENV=production PORT=3000 DATA_ROOT=/var/lib/helixcache PYTHON_BIN=python3 PIP_NO_CACHE_DIR=1
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends python3 python3-pip && rm -rf /var/lib/apt/lists/*
COPY requirements-inference.txt ./
RUN pip3 install --break-system-packages -r requirements-inference.txt
COPY package.json ./
COPY src ./src
COPY public ./public
RUN mkdir -p /var/lib/helixcache && chown -R node:node /app /var/lib/helixcache
USER node
EXPOSE 3000
CMD ["node", "src/server.js"]
